version: 1

Resources:
  - TargetService:
      Type: AWS::ECS::Service
      properties:
        TaskDefinition: <TASK_DEFINITION>
        LoadBalancerInfo:
          ContainerName: strapi-container
          ContainerPort: 1337